name: FX System CI/CD Pipeline

on:
  push:
    branches: [ main, feature/* ]
  pull_request:
    branches: [ main ]

jobs:
  # 1. Backend CI: Runs Python Tests
  backend-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Backend Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-flask

      - name: Run Pytest Suite
        run: |
          # Adding backend to PYTHONPATH so imports work
          export PYTHONPATH=$PYTHONPATH:$(pwd)/backend
          pytest tests/ -v

  # 2. Frontend CI: Builds React App
  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './frontend/package-lock.json'

      - name: Install Frontend Dependencies
        working-directory: ./frontend
        run: npm install

      - name: Build React App
        working-directory: ./frontend
        run: npm run build

  # 3. CD: Deployment (Placeholder for common providers)
  # Remove comments and configure if you use Render/Railway/AWS
  # deploy:
  #   needs: [backend-test, frontend-build]
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Deploy to Production
  #       run: echo "Triggering deployment to hosting provider..."
